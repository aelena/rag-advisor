[build-system]
requires = ["setuptools>=77.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ragadvisor"
version = "0.3.2"
description = "RAG Configuration Adviser — rule-based recommendations for building RAG systems"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Anton" },
]
keywords = ["rag", "retrieval-augmented-generation", "cli", "advisor", "embeddings"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    # CLI & UI
    "typer>=0.9.0",
    "rich>=13.0.0",
    # Document analysis
    "langdetect>=1.0.9",
    "chardet>=5.0.0",
    "tiktoken>=0.5.0",
    "pypdf>=3.0.0",
    "python-docx>=1.0.0",
    # HuggingFace model discovery
    "huggingface-hub>=0.20.0",
    # Config & data
    "pyyaml>=6.0",
    # XML parsing (safe)
    "defusedxml>=0.7.0",
    # Report generation
    "jinja2>=3.1.0",
    # HTTP client for LLM API calls
    "httpx>=0.24.0",
]

[project.optional-dependencies]
spacy = [
    "spacy>=3.6.0",
]
eval = [
    "sentence-transformers>=2.2.0",
    "einops>=0.7.0",
    "chromadb>=0.4.0",
]
eval-faiss = [
    "sentence-transformers>=2.2.0",
    "einops>=0.7.0",
    "faiss-cpu>=1.7.0",
]
eval-pgvector = [
    "sentence-transformers>=2.2.0",
    "einops>=0.7.0",
    "psycopg[binary]>=3.1.0",
    "pgvector>=0.2.0",
]
eval-sqlite = [
    "sentence-transformers>=2.2.0",
    "einops>=0.7.0",
    "sqlite-vec>=0.1.0",
]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "pytest-mock>=3.10",
    "ruff>=0.1.0",
]
all = [
    "ragadvisor[spacy,eval,dev]",
]

[project.scripts]
ragadvisor = "rag_adviser.cli:app"

[project.urls]
Homepage = "https://github.com/aelena/rag-advisor"
Repository = "https://github.com/aelena/rag-advisor"
Issues = "https://github.com/aelena/rag-advisor/issues"
Changelog = "https://github.com/aelena/rag-advisor/blob/main/CHANGELOG.md"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
rag_adviser = [
    "templates/*",
    "config/*.yaml",
    "research/papers/*.md",
    "research/ground_truth.jsonl",
    "presets/builtin/*.yaml",
]

[tool.ruff]
target-version = "py310"
line-length = 100
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "A", "C4", "SIM"]

[tool.ruff.lint.per-file-ignores]
# Catalogue of ingestion advice: long prose strings are the content, not code.
"src/rag_adviser/recommenders/modality_recommender.py" = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
